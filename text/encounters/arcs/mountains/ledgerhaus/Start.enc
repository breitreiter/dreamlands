Placeholder: The Ledgerhaus

A placeholder encounter for the Ledgerhaus arc.

choices:

* Explore = Search the archives
    @if check combat medium {
        You find something valuable.
        +add_item petitioners_writ
        +finish_dungeon
    } @else {
        It doesn't go well.
        +add_condition injured
        +finish_dungeon
    }

* Negotiate = Talk your way through
    @if check negotiation medium {
        You find something valuable.
        +add_item petitioners_writ
        +finish_dungeon
    } @else {
        It doesn't go well.
        +add_condition injured
        +finish_dungeon
    }
