Placeholder: The Harrow Line
[trigger none]

A placeholder encounter for the Harrow Line arc.

choices:

* Explore = Search the battlefield [requires quality faction.scavengers 5]
    @if check combat medium {
        You find something valuable.
        +add_item lucky_buckle
        +finish_dungeon
    } @else {
        It doesn't go well.
        +add_condition injured
        +finish_dungeon
    }

* Negotiate = Talk your way through [requires quality faction.scavengers 5]
    @if check negotiation medium {
        You find something valuable.
        +add_item lucky_buckle
        +finish_dungeon
    } @else {
        It doesn't go well.
        +add_condition injured
        +finish_dungeon
    }

* Depart = Leave this place behind
    You depart Harrow Line.
    +flee_dungeon
