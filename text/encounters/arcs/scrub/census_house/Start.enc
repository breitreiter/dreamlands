Placeholder: The Census House

A placeholder encounter for the Census House arc.

choices:

* Explore = Search the records
    @if check combat medium {
        You find something valuable.
        +upgrade_pack 1
        +finish_dungeon
    } @else {
        It doesn't go well.
        +add_condition injured
        +finish_dungeon
    }

* Negotiate = Talk your way through
    @if check negotiation medium {
        You find something valuable.
        +upgrade_pack 1
        +finish_dungeon
    } @else {
        It doesn't go well.
        +add_condition injured
        +finish_dungeon
    }
